(ns zipper-snippets.weird
  (:require
    [clojure.zip :as zip]))

;; abuse of zippers...
(zip/zipper
  ; branch?
  ; children
  ; make-node
  ; root
  )

;; move (insert then remove)
;; splice multiple elements into loc, removing some?
;; range
;;   slice/remove
;;   edit
;;
